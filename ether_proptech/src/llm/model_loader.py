# Архитектура системы "Эфир": Контекст и Взаимодействие

## 1. Обзор (C4 Model - Context Level)
Система "Эфир" представляет собой локально развернутый контур управления недвижимостью, где ИИ выступает в роли строгого, но справедливого регулятора среды.

### Основные акторы
- **Резидент:** Пользователь приложения, получатель услуг, источник запросов.
- **Подрядчик:** Внешний исполнитель, контролируемый системой.
- **Администратор:** Сотрудник УК, действующий по инструкциям системы.
- **Физическая среда:** Здание, оснащенное IoT-датчиками (шум, воздух, доступ).

## 2. Логическая архитектура

### Слой 1:感知 (Perception Layer)
- **IoT Sensors:** Датчики шума (дБА), качества воздуха (PM2.5, CO2, VOC), камеры (только метаданные, без записи лиц).
- **Gateway:** Локальный шлюз, агрегирующий данные и отправляющий триггеры в API.

### Слой 2: Intelligence & Logic (Core)
- **RAG Engine:** Ядро знаний. Хранит векторное представление законов (ГОСТ, ЖК РФ) и внутренних регламентов.
- **LLM Inference (Local):** Модель Qwen/DeepSeek, работающая оффлайн. Генерирует решения только на основе контекста RAG.
- **Validator Module:** "Страж". Проверяет каждый ответ LLM на наличие цитат. Блокирует ответы без ссылок на источники.
- **Workflow Engine:** Машина состояний для управления инцидентами (Новый -> В работе -> Эскалация -> Закрыто).

### Слой 3: Data Persistence (Docs-as-Code)
- **Git Repository:** Единый источник истины для регламентов. Любое изменение правила — это коммит с ревью.
- **Vector DB (Chroma):** Индексированное знание для быстрого поиска.
- **Time-series DB:** История показаний датчиков (неизменяемый лог).

### Слой 4: Interface
- **Resident App:** Уведомления, чат с "Законодателем", жалобы.
- **Admin Dashboard:** Карта инцидентов, управление доступом, аналитика SLA.
- **API Gateway:** RESTful интерфейс для интеграций.

## 3. Поток данных при инциденте (Noise Violation)
1. **Датчик** фиксирует > 55 дБА ночью.
2. **Gateway** отправляет событие в **API**.
3. **Workflow Engine** создает инцидент P1.
4. **RAG Engine** ищет в базе: "Какие действия предписаны при шуме ночью?".
5. **LLM** генерирует текст уведомления со ссылкой на [ГОСТ 31319-2009].
6. **Validator** подтверждает наличие ссылки.
7. **Система** отправляет уведомление жителю и блокирует гостевой доступ.
8. Данные записываются в **Git** (лог инцидента) и **Time-series DB**.

## 4. Требования к безопасности и приватности
- **Data Sovereignty:** Все данные хранятся локально (on-premise). Облачные передачи запрещены.
- **Privacy by Design:** Камеры не используются для распознавания лиц, только для детекции движения/скопления людей (опционально). Основной упор на акустические и химические сенсоры.
- **Auditability:** Каждое решение системы можно отследить до конкретного коммита в репозитории регламентов.

## 5. Масштабируемость
- Архитектура модульная. Добавление нового дома = развертывание нового экземпляра контейнеров с уникальной базой регламентов.
- Использование легких моделей (7B параметров) позволяет работать на недорогом серверном оборудовании (например, мини-ПК с GPU).